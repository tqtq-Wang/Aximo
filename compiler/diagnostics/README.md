# compiler/diagnostics

Reserved work area for structured diagnostics.

Planned responsibilities:

- code namespaces
- location handling
- formatter and JSON emission
- schema alignment with `schemas/diagnostics.schema.json`
